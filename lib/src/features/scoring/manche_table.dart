import 'package:flutter/material.dart';

class MancheTable extends StatelessWidget {
  const MancheTable({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "La fonctionnalité de tableau des manches n'est plus disponible.",
      ),
    );
  }
}
